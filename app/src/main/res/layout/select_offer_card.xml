<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    android:id="@+id/offerContainer"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content" >

    <ImageView
        android:id="@+id/offerImage"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:adjustViewBounds="true"
        app:layout_constraintBottom_toTopOf="@+id/offerTitle"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginBottom="8dp"
        tools:scaleType="centerCrop"
        tools:src="@drawable/auth_background" />

    <TextView
        android:id="@+id/offerTitle"
        style="@style/Models15SemiBoldText"
        android:includeFontPadding="false"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:ellipsize="end"
        app:layout_constraintBottom_toTopOf="@+id/offerCreditsLl"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        tools:text="Entrance" />

    <LinearLayout
        android:id="@+id/offerCreditsLl"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" >

        <TextView
            android:id="@+id/offerPrice"
            style="@style/Models14SemiBoldText"
            android:textColor="@color/nice_pink"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            tools:text="250" />

        <TextView
            style="@style/Models14Text"
            android:textColor="@color/nice_pink"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:layout_marginStart="4dp"
            android:text="@string/credits_lowercase" />
    </LinearLayout>

    <RelativeLayout
        android:id="@+id/notAvailableRl"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="@+id/offerImage"
        app:layout_constraintBottom_toBottomOf="@+id/offerImage"
        app:layout_constraintStart_toStartOf="@+id/offerImage"
        app:layout_constraintEnd_toEndOf="@+id/offerImage">
        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/black_trans_50" />
        <TextView
            android:id="@+id/offerAvailableText"
            style="@style/Models14Text"
            android:textColor="@android:color/white"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_alignParentTop="true"
            android:layout_alignParentStart="true"
            android:layout_alignParentEnd="true"
            android:maxLines="1"
            android:gravity="center"
            tools:text="Not available from 13:00 to 15:00" />
    </RelativeLayout>

    <View
        android:id="@+id/checkedOutline"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"
        tools:visibility="visible"
        app:layout_constraintTop_toTopOf="@+id/offerImage"
        app:layout_constraintBottom_toBottomOf="@+id/offerImage"
        app:layout_constraintStart_toStartOf="@+id/offerImage"
        app:layout_constraintEnd_toEndOf="@+id/offerImage"
        android:background="@drawable/outline_10_px" />

    <FrameLayout
        android:id="@+id/checkedImv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        tools:visibility="visible"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="parent">
        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginTop="4dp"
            android:layout_marginEnd="4dp"
            android:src="@drawable/ic_check_circle_whiteinside" />
    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>